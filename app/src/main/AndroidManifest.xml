<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.martynaskairys.wallpee"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.SET_WALLPAPER"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.DarkActionBar">
        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".ChoosingFolderActivity">
        </activity>
        <activity android:name=".ExplainingChosenFolderActivity">
        </activity>
        <activity android:name=".PictureActivity">
        </activity>
        <activity android:name=".ThumbnailActivity"/>
        <activity android:name=".ChoosingRandomPicture">
        </activity>
    </application>

</manifest>
